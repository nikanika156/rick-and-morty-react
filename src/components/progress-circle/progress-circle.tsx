import { useEffect, useState } from "react";

interface Props {
  max: number;
  value: number;
}
export function ProgressCircle({ max, value }: Props) {
  max;
  value;
  return (
    <div>

    </div>
  );
}
